=> TECH STACK:

    A). Frontend: React.js
    B). Backend: Node.js, Express.js
    C). Database: MongoDB
    D). Authentication: JWT (JSON Web Token)
    E). State Management: Context API and React useState
    F). Styling: CSS
    
=>FEAUTURES:
    
    A). User Registration and Login with JWT Authentication
    B). Error handling for invalid credentials or unregistered users
    C). Protected dashboard accessible only to logged-in users
    D). Category Management Dashboard
            * View categories
    E). Logout button in the header for secure session handling


=>How to Run Locally?:

      step 1: git clone <Repository url>
      step 2: cd myapp
      step 3 : npm start
    
